{
  "name": "yii2-cms-videojs-10-widget",
  "version": "1.0.0",
  "private": true,
  "description": "Build scripts for VideoJS v10 widget",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --watch"
  },
  "dependencies": {
    "@videojs/html": "^10.0.0-beta"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.8.0"
  }
}
